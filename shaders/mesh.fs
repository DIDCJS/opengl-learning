#version 330 core  
// in vec3 Normal;
// in vec3 FragPos;
// in vec2 TexCoords;
// uniform vec3 lightPos;
// uniform vec3 objectColor;
// uniform vec3 viewPos;

void main(){
	gl_FragColor = vec4(1.0);

}